default:

  debug_mode: False

  app:
    name: "Chrome"
    description: "Google Chrome is a web browser for Android that lets users visit sites, manage tabs, and sync bookmarks and passwords."

  driver:
    device_id: "ZY22LR9N8G"
    os_name: "android"
    appPackage: "com.android.chrome"
    appActivity: "com.google.android.apps.chrome.Main"
    skip_exploration_for_no_app_package: true
    reset_instruction: "Go to a new tab page or the main browser view. If already there, return finished."

  vlm:
    alibaba_api_key: 'sk-9e08c5810b434d9db9a335e1cca2a02a'
    yibu_api_key: 'sk-J56s8hEAdQfWk6PyWxdkbCzH644eu2L7O5OUehIW1QM93YU2'
    use_yibu_api: true

  agent:
    verbose_print: false
    url: "http://10.218.163.104:8000/v1"
    model_name: "mai_ui"
    settings: 
      history_n: 3
      resize_factor: 0.5
      model_thinking: true
      max_tokens_no_thinking: 128
    max_steps: 10

  logs:
    root: "../exploration/explored_apps/chrome"
    resume_from_checkpoint: true

  batch_mode: false
  output_dir: "./results"

  use_memory_for_navigation: true
  query: ""
  top_k_in_first_stage_retrieval: 30
  top_k_retrieval_in_stage_2: 5
  pick_candidate_index: 0    # 0 means best candidate, 1 means second best, ... -1 means use gradio to pick candidate  (has to be less than top_k_retrieval_in_stage_2)

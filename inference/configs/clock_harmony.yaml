default:

  app:
    name: "Clock"
    description: "The Clock app provides users with tools to manage time through alarms, timers, a stopwatch, and world clocks."

  driver:
    device_id: "3AP0224B05003112"
    os_name: "harmony"
    appPackage: "com.huawei.hmos.clock"
    appActivity: "com.huawei.hmos.clock.phone"  
    skip_exploration_for_no_app_package: true # so if this is True we skip exploration if we are not in the app package for example if we are in clock app and we opened browser (using help button  we don't want to explore there or if we land on home screen))
    reset_instruction: "Go Alarm tab. (if Alarm tab is highlighted (active) you don't need to to anything just return Finish)"


  vlm:
    alibaba_api_key: 'sk-9e08c5810b434d9db9a335e1cca2a02a'
    yibu_api_key: 'sk-caIdHUNLbUgJ4vpB3LFT2FQhpV4ELNgmL5PXBGgJq1kvLL8C'
    use_yibu_api: true



  agent:
    verbose_print: false
    url: "http://10.193.241.177:8090/v1"
    model_name: "mai_ui"
    settings: 
      history_n: 3
      resize_factor: 0.75

  logs:
    root: "../exploration/explored_apps/clock_harmony"
    resume_from_checkpoint: true

  use_memory_for_navigation: false
  query: "Navigate to the page that i can change the Repeat of the alarm sound occurance."
  top_k_in_first_stage_retrieval: 15
  top_k_retrieval_in_stage_2: 5




default:

  debug_mode: False

  app:
    name: "Yahoo Finance"
    description: "Yahoo Finance is a mobile app for tracking stocks, market news, portfolios, and financial data."

  driver:
    device_id: "R3CX20L5N6F"
    os_name: "android"
    appPackage: "com.yahoo.mobile.client.android.finance"
    appActivity: ".main.MainActivity"
    skip_exploration_for_no_app_package: true # If true, skip exploration when not in the Yahoo Finance app package (e.g., after navigating away).
    skip_scroll_up_on_reset: true

  vlm:
    alibaba_api_key: 'sk-9e08c5810b434d9db9a335e1cca2a02a'
    yibu_api_key: 'sk-caIdHUNLbUgJ4vpB3LFT2FQhpV4ELNgmL5PXBGgJq1kvLL8C'
    use_yibu_api: true

  agent:
    verbose_print: false
    url: "http://10.193.241.177:8090/v1"
    model_name: "mai_ui"
    settings: 
      history_n: 3
      resize_factor: 0.75
      model_thinking: true
    max_steps: 10

  logs:
    root: "../exploration/explored_apps/yahoo_finance"
    resume_from_checkpoint: true

  batch_mode: true
  output_dir: "./results"

  use_memory_for_navigation: true
  query: "Navigate to the Feedback to Microsoft page."
  top_k_in_first_stage_retrieval: 30
  top_k_retrieval_in_stage_2: 5
  pick_candidate_index: 0    # 0 means best candidate, 1 means second best, ... -1 means use gradio to pick candidate  (has to be less than top_k_retrieval_in_stage_2)
